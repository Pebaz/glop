// Bins are just CLIs to lib functionality?
// Need to have fast path for compiling lots of source files
// Don't use many processes, use many cores

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
