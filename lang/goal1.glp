@clear-screen(),

let x = 10,
let y = 64,

loop
[
    if @u64-gte(10000000000, 1000)
    [
        @draw-pixel(8, 8, 255, 0, 55),

        break,
    ]
    else
    [
        @draw-pixel(x, y, 255, 255, 255),

        set x = @u64-add(x, 1),
    ]
]